services:
  - type: web
    name: nft-security-analyzer
    env: python
    rootDir: backend
    buildCommand: |
      apt-get update && apt-get install -y graphviz
      pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
